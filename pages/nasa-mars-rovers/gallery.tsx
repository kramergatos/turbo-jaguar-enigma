import Layout from '../../components/Layout'
import Form from '../../components/Nasa/Form'
import styles from '../../styles/Home.module.css'
function Gallery({data}) {
  return (
    <Layout pageTitle="NASA">
      <Form />
    </Layout>
  )
}
export default Gallery
